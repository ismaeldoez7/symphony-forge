{
  "generated_by": "claude-code:plan-mode",
  "user_facing": false,
  "story": "FORGE-TERSE-1",
  "epic": "terse-output",
  "plan_file": "plans/active/FORGE-TERSE-1-terse-cli-success-paths-budget-test.md",
  "doc_roots": [
    "docs/specs/terse-output.md"
  ],
  "tasks": [
    {
      "id": "FORGE-TERSE-1.1",
      "title": "Terse CLI success paths + budget test",
      "objective": "Every touched forge command's success prints one line (state change + identifiers); all coaching lives in forge next and full errors; a budget gate test pins the line counts so lectures cannot creep back. No guidance is lost - five otherwise-lost messages move into forge next.",
      "dependencies": [],
      "write_scope": [
        "factory/scripts/forge_cli/stages.py",
        "factory/scripts/forge_cli/plans.py",
        "factory/scripts/forge_cli/decisions.py",
        "factory/scripts/forge_cli/context.py",
        "factory/scripts/forge_cli/delegate.py",
        "factory/scripts/forge_cli/roadmap.py",
        "factory/scripts/forge_cli/signal.py",
        "factory/scripts/forge_cli/doctor.py",
        "factory/scripts/forge_cli/sanitise.py",
        "factory/scripts/forge_cli/phase.py",
        "factory/scripts/forge_cli/common.py",
        "factory/scripts/intake.py",
        "factory/scripts/pr_ready.py",
        "factory/scripts/record_decomposition_from_json.py",
        "factory/scripts/record_signoff.py",
        "factory/tests/test_gates.py"
      ],
      "acceptance_criteria": [
        "The 12 pure-coaching deletions, 7 mixed-line trims, 4 multi-result folds, and the delegate one-line collapse land exactly per the plan's site list; folded lines preserve the 11 asserted substrings verbatim (marked active, PR_READY, Accepted, Superseded, pinned to, Write access:, the argv, not launched, pending: N, stages.json, debt cleared).",
        "phase.py gains the five moved steps in the right branches: commit-the-evidence (post-ship), commit-the-pin (signed-off), decision hygiene (trailer command + supersedes flip + confirmed_by), spec-debt clearance (post-sign-off); no other coaching is added.",
        "Documented exceptions kept: signal raise's worker-facing PAUSE line and stage done's contract-change NOTE; machine contracts untouched: doctor failure last-line, context list per-entry lines, the 'forge doctor: ready' prefix.",
        "Error and refusal messages have zero diff; forge next output is the only place coaching grows.",
        "test_success_output_budget pins per-command success line budgets; test_gates.py:3653, 7025, 7038, 8915 updated to the new shapes; common.py carries the one-line convention docstring."
      ],
      "verify_commands": [
        "uv run --with pytest python -m pytest factory/tests/test_gates.py -q -k \"output_budget or success or delegate_chrome or signal or decision\""
      ],
      "required_tests": [
        {
          "id": "test_success_output_budget",
          "path": "factory/tests/test_gates.py",
          "command": "uvx --with pytest python3 -m pytest {path}::{id} -q -o junit_family=legacy --junitxml={report}"
        }
      ],
      "reviewer_focus": "No refusal/error string changes anywhere in the diff; folded success lines keep the asserted substrings byte-verbatim; the five forge next additions match the deleted coaching in content (moved, not paraphrased into something weaker); no result() helper or verbosity flag sneaks in; doctor's failure path and context list output untouched."
    }
  ],
  "project": "",
  "plan_sha256": "abd609bd2f2a757d4be466c74d49b592314dd7f940f9c6b22a3f958d608dd38d",
  "commit": "4caee03523fa927ff3d1491a7f119430eb55381e"
}
